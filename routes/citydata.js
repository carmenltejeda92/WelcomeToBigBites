const express = require('express');
const router = express.Router();
const fetchCityDatacityData = require('../src/utilities/city-api');

router.post('/saveData', async (req, res) => {
    try {
        const cityData = await 
    })

module.exports = router
